@model Pasifika.WebUI.Areas.Admin.ViewModels.CiudadListViewModel
@{
    ViewBag.Title = "List";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<!-- Widget ID (each widget will need unique ID)-->
<div class="jarviswidget" id="wid-id-0">
    <header>
        <span class="widget-icon"> <i class="fa fa-edit"></i> </span>
        <h2>Ciudades </h2>

    </header>

    <!-- widget div-->
    <div>

        <!-- widget edit box -->
        <div class="jarviswidget-editbox">
            <!-- This area used as dropdown edit box -->
        </div>
        <!-- end widget edit box -->
        <!-- widget content -->
        <div class="widget-body no-padding">
            <div class="row">
                <div class="col-lg-12">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th class="col-lg-1">Código</th>
                                <th class="col-lg-11">Ciudad</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var c in Model.Ciudades)
                            {
                                <tr data-id="@c.Id">
                                    <td>
                                        @Html.ActionLink(c.Id.ToString(), "Edit", new
                                            {
                                                id = c.Id
                                            })
                                    </td>
                                    <td>@c.Nombre</td>
                                    <td>
                                        @using (Html.BeginForm("Delete", "Ciudad"))
                                        {
                                            @Html.Hidden("Id", c.Id)
                                            <input type="submit" class="btn btn-default btn-xs" value="Delete" />
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="widget-footer padding-bottom-10">
                @Html.ActionLink("Nueva Ciudad", "Create", new { destinoId = Model.Destino.Id }, new { @class = "btn btn-primary" })
            </div>
        </div>
    </div>
    <!-- end widget content -->
</div>
<!-- end widget div -->